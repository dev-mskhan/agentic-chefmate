{
  "name": "chefmate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test:api": "pnpm --filter @chefmate/api-tests exec playwright test",
    "test:api:auth": "pnpm --filter @chefmate/api-tests exec playwright test tests/auth",
    "test:api:gateway": "pnpm --filter @chefmate/api-tests exec playwright test tests/gateway",
    "test:api:ci": "pnpm --filter @chefmate/api-tests exec playwright test --reporter=line",
    "test:api:debug": "pnpm --filter @chefmate/api-tests exec playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.9.0",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/nodemailer": "^8.0.1",
    "turbo": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
